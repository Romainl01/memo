// Initialize globals needed for React Native testing
// These must be defined before react-native is imported

// Define __DEV__ flag
global.__DEV__ = true;

// Initialize Expo's import meta registry
globalThis.__ExpoImportMetaRegistry = globalThis.__ExpoImportMetaRegistry || {};
